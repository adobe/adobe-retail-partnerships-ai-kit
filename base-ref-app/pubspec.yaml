name: partners_ref_app
description: Partner Ref App — feature-less base app (login + session + home shell) the Partner Integration AI Kit builds the Adobe integration onto
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  dio: ^5.9.0
  go_router: ^17.3.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
