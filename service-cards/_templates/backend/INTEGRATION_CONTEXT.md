# Integration Context — {service_name}

*Where the Adobe integration actually lives inside this backend surface.
This is the map the LLD and feature steps use to place new code correctly. Fill
every item from the partner's real code, citing file paths; where the code does
not make it clear, mark `TODO — ASK PARTNER` (verification rule 8). Follows
[`../../../manual/methodology/card-model.md`](../../../manual/methodology/card-model.md) §4.2.*

---

## Where `partner_reference_id` Is Stored

*The entity/column/field that holds `partner_reference_id`, and where it is
generated. Recall the invariant: it is permanent per (customer, product) pair —
a new one is generated after cancel + re-purchase.*

- **Stored in:** `<fill: table.column / collection.field>`
- **Generated by:** `<fill: file / method>`
- **Format:** `<fill: the partner's existing customer/order/subscription ID
  convention if one exists (cite it); if none exists, ASK the partner what
  format/constraints apply (max length, allowed characters, no embedded
  customer data) before defaulting to a fresh UUID — see
  ../../../knowledgebase/integration-patterns/idempotency.md. A UUID default
  is a placeholder, not an assumption to skip past.>`
- **Notes:** TODO

## Where Subscription State Lives

*The entity/table/collection that tracks subscription status (`ACTIVE` /
`CANCELLED`) and how it maps to Adobe's status enum.*

- **Entity / table / collection:** `<fill>`
- **Status field & values:** `<fill>`
- **Updated by:** `<fill: claim / cancel / notify handlers>`
- **Notes:** TODO

## Deep-link / Return-from-Adobe Model

*How the user returns to the partner after the Adobe claim workflow — the return
URL, deep link, or callback route this backend expects or issues.*

- <fill> — TODO

## Adobe Redirect / Return Assumptions

*What this backend assumes about Adobe's redirect/return behavior — query params,
tokens, or state it expects on return, and where that is handled.*

- <fill> — TODO

## Which Backend Owns the Adobe Integration (multi-repo)

*In a multi-repo/multi-service setup, which service owns the IMS + Retail API
calls and the Notify endpoint. If this is the owning backend, say so; if not,
name the one that is. See [`../../../manual/methodology/modes.md`](../../../manual/methodology/modes.md#surface-registry-and-multi-repo-resolution).*

- **Owning backend:** `<fill: this service | other service name>`
- **This service's role:** `<fill>`
- **Notes:** TODO

## UI Rendering (SSR / monolithic partners only)

*Fill this section ONLY if the UI is server-rendered inside this SAME
process/repo as the backend (e.g. Rails views, Django templates, a Next.js app
doing SSR+API together) with no separate client-side data-fetch layer — see
[card-model.md § the three partner surfaces](../../../manual/methodology/card-model.md).
In that case no separate UI card set is produced; this subsection is the only
place the rendering path is recorded. Leave this section out entirely (not
`TODO`) when a separate UI surface has its own card set.*

- **Templates / views rendering the claim screen:** `<fill>`
- **Templates / views rendering the subscription-status screen:** `<fill>`
- **Templates / views rendering the cancel screen:** `<fill>`
- **How these read backend state directly:** `<fill>` (e.g. the
  controller/handler passes subscription state straight into the template
  context — no separate `DATA_LAYER.md` needed)
- **Notes:** TODO
